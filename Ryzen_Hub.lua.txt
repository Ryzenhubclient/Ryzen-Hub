-- RYZEN HUB - MM2 ПОЛНАЯ ВЕРСИЯ
local player = game.Players.LocalPlayer
local mouse = player:GetMouse()

local settings = {
    bunnyhop = 100,
    flyspeed = 50,
    jumppower = 120,
    spinSpeed = 10,
    weatherType = "Sunny",
}

local toggles = {
    esp = false,
    showMurderer = true,
    showSheriff = true,
    showInnocent = false,
    triggerbot = false,
    bunnyhop = false,
    jump = false,
    noclip = false,
    teleport = false,
    fly = false,
    showFps = false,
    autoCollectGun = false,
    spinBot = false,
    fling = false,
    bombMode = false,
    shotMurderer = false,
    fakeRoblox = false,
    headless = false,
    customClouds = false,
    customWeather = false,
    masturbate = false,
}

local playerListGui = nil
local espObjects = {}
local fpsLabel = nil
local bombActive = false
local bombTimer = 0
local bombPart = nil
local fakeNameLabel = nil
local clouds = {}

local noclipActive, noclipStartTime, noclipConnection = false, 0, nil
local triggerbotConnection, bunnyHopConnection, spinConnection, flingConnection, flyConnection = nil, nil, nil, nil, nil
local masturbateConnection = nil

-- ===== ДРОЧКА =====
local function toggleMasturbate(state)
    toggles.masturbate = state
    if state then
        print("🍆 Режим дрочки включён!")
        masturbateConnection = game:GetService("RunService").RenderStepped:Connect(function()
            if not toggles.masturbate then
                if masturbateConnection then masturbateConnection:Disconnect() end
                return
            end
            if player.Character and player.Character:FindFirstChild("Right Arm") then
                local arm = player.Character["Right Arm"]
                local offset = math.sin(tick() * 8) * 0.3
                arm.CFrame = arm.CFrame * CFrame.new(0, offset, 0)
            end
        end)
    else
        print("🍆 Режим дрочки выключен.")
        if masturbateConnection then
            masturbateConnection:Disconnect()
            masturbateConnection = nil
        end
        if player.Character and player.Character:FindFirstChild("Right Arm") then
            player.Character["Right Arm"].CFrame = player.Character["Right Arm"].CFrame * CFrame.new(0, 0, 0)
        end
    end
end

-- ===== ВИЗУАЛ: FAKE ROBLOX =====
local function toggleFakeRoblox(state)
    toggles.fakeRoblox = state
    if state then
        if not fakeNameLabel then
            local gui = Instance.new("ScreenGui")
            gui.Name = "FakeRobloxGUI"
            gui.Parent = game.CoreGui
            fakeNameLabel = Instance.new("TextLabel")
            fakeNameLabel.Size = UDim2.new(0, 200, 0, 30)
            fakeNameLabel.Position = UDim2.new(0, 10, 0, 10)
            fakeNameLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            fakeNameLabel.BackgroundTransparency = 0.5
            fakeNameLabel.BorderSizePixel = 0
            fakeNameLabel.Text = "ROBLOX | " .. player.Name
            fakeNameLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            fakeNameLabel.TextScaled = true
            fakeNameLabel.Font = Enum.Font.GothamBold
            fakeNameLabel.Parent = gui
        end
    else
        if fakeNameLabel then
            fakeNameLabel.Parent:Destroy()
            fakeNameLabel = nil
        end
    end
end

-- ===== ВИЗУАЛ: HEADLESS =====
local function toggleHeadless(state)
    toggles.headless = state
    if state then
        if player.Character and player.Character:FindFirstChild("Head") then
            player.Character.Head.Transparency = 1
        end
        player.CharacterAdded:Connect(function(char)
            if toggles.headless then
                task.wait(0.5)
                if char and char:FindFirstChild("Head") then
                    char.Head.Transparency = 1
                end
            end
        end)
    else
        if player.Character and player.Character:FindFirstChild("Head") then
            player.Character.Head.Transparency = 0
        end
    end
end

-- ===== ВИЗУАЛ: КАСТОМНЫЕ ОБЛАКА =====
local function createCustomClouds()
    for i = 1, 10 do
        local cloud = Instance.new("Part")
        cloud.Size = Vector3.new(math.random(20, 50), math.random(3, 8), math.random(20, 50))
        cloud.Position = Vector3.new(math.random(-200, 200), math.random(100, 200), math.random(-200, 200))
        cloud.BrickColor = BrickColor.new("White")
        cloud.Material = Enum.Material.Neon
        cloud.Anchored = true
        cloud.CanCollide = false
        cloud.Transparency = 0.3
        cloud.Parent = workspace
        table.insert(clouds, cloud)
    end
end

local function removeCustomClouds()
    for _, cloud in ipairs(clouds) do
        if cloud and cloud.Parent then
            cloud:Destroy()
        end
    end
    clouds = {}
end

local function toggleCustomClouds(state)
    toggles.customClouds = state
    if state then
        removeCustomClouds()
        createCustomClouds()
    else
        removeCustomClouds()
    end
end

-- ===== ВИЗУАЛ: ПОГОДА =====
local function toggleCustomWeather(state)
    toggles.customWeather = state
    if state then
        local lighting = game:GetService("Lighting")
        if settings.weatherType == "Rain" then
            lighting.ClockTime = 12
            lighting.Brightness = 0.5
            game:GetService("Workspace").Terrain.WaterWaveSize = 0.5
            game:GetService("Workspace").Terrain.WaterWaveSpeed = 0.5
        elseif settings.weatherType == "Foggy" then
            lighting.FogEnd = 50
            lighting.FogColor = Color3.fromRGB(150, 150, 150)
        elseif settings.weatherType == "Night" then
            lighting.ClockTime = 0
            lighting.Brightness = 0.2
        else
            lighting.ClockTime = 14
            lighting.Brightness = 1
            lighting.FogEnd = 1000
        end
    else
        local lighting = game:GetService("Lighting")
        lighting.ClockTime = 14
        lighting.Brightness = 1
        lighting.FogEnd = 1000
        lighting.FogColor = Color3.fromRGB(255, 255, 255)
        game:GetService("Workspace").Terrain.WaterWaveSize = 0
        game:GetService("Workspace").Terrain.WaterWaveSpeed = 0
    end
end

-- ===== МИНИ-МЕНЮ ИГРОКОВ =====
local function togglePlayerList()
    if playerListGui then
        playerListGui:Destroy()
        playerListGui = nil
        return
    end

    playerListGui = Instance.new("ScreenGui")
    playerListGui.Name = "PlayerList"
    playerListGui.Parent = game.CoreGui

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 200, 0, 300)
    frame.Position = UDim2.new(0.7, 0, 0.3, 0)
    frame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 0
    frame.Parent = playerListGui

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 30)
    title.BackgroundTransparency = 1
    title.Text = "ИГРОКИ"
    title.TextColor3 = Color3.fromRGB(255, 70, 70)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = frame

    local list = Instance.new("ScrollingFrame")
    list.Size = UDim2.new(1, 0, 1, -30)
    list.Position = UDim2.new(0, 0, 0, 30)
    list.BackgroundTransparency = 1
    list.Parent = frame

    local function updateList()
        for _, child in ipairs(list:GetChildren()) do
            if child:IsA("TextButton") then child:Destroy() end
        end

        local yPos = 0
        for _, plr in ipairs(game.Players:GetPlayers()) do
            if plr ~= player then
                local btn = Instance.new("TextButton")
                btn.Size = UDim2.new(1, 0, 0, 30)
                btn.Position = UDim2.new(0, 0, 0, yPos)
                btn.BackgroundColor3 = Color3.fromRGB(55, 55, 60)
                btn.BorderSizePixel = 0
                btn.Text = plr.Name
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
                btn.TextScaled = true
                btn.Font = Enum.Font.Gotham
                btn.Parent = list

                btn.MouseButton1Click:Connect(function()
                    if plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        plr.Character.HumanoidRootPart.CFrame = CFrame.new(0, -500, 0)
                        print("🚀 Игрок " .. plr.Name .. " выкинут за карту!")
                    end
                end)
                yPos = yPos + 30
            end
        end
    end

    updateList()
    game.Players.PlayerAdded:Connect(updateList)
    game.Players.PlayerRemoving:Connect(updateList)
end

-- ===== ОПРЕДЕЛЕНИЕ РОЛИ =====
local function getPlayerRole(plr)
    if not plr or not plr.Character then return "Innocent" end
    local hasKnife, hasGun = false, false
    for _, tool in ipairs(plr.Character:GetChildren()) do
        if tool:IsA("Tool") then
            local name = tool.Name:lower()
            if name:find("knife") or name:find("dagger") then hasKnife = true end
            if name:find("gun") or name:find("pistol") or name:find("revolver") then hasGun = true end
        end
    end
    if plr.Backpack then
        for _, tool in ipairs(plr.Backpack:GetChildren()) do
            if tool:IsA("Tool") then
                local name = tool.Name:lower()
                if name:find("knife") or name:find("dagger") then hasKnife = true end
                if name:find("gun") or name:find("pistol") or name:find("revolver") then hasGun = true end
            end
        end
    end
    if hasKnife and hasGun then return "Sheriff"
    elseif hasKnife then return "Murderer"
    elseif hasGun then return "Sheriff"
    else return "Innocent" end
end

-- ===== ESP =====
local function updateESP()
    for _, obj in ipairs(espObjects) do
        if obj and obj.Parent then obj:Destroy() end
    end
    espObjects = {}
    if not toggles.esp then return end

    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr == player or not plr.Character or not plr.Character:FindFirstChild("Head") then continue end
        local role = getPlayerRole(plr)
        local show = false
        if role == "Murderer" and toggles.showMurderer then show = true end
        if role == "Sheriff" and toggles.showSheriff then show = true end
        if role == "Innocent" and toggles.showInnocent then show = true end
        if not show then continue end

        local color = role == "Murderer" and Color3.fromRGB(255, 0, 0) or 
                     role == "Sheriff" and Color3.fromRGB(0, 100, 255) or 
                     Color3.fromRGB(0, 255, 0)

        local esp = Instance.new("BillboardGui")
        esp.Name = "ESP_Ryzen"
        esp.Size = UDim2.new(0, 150, 0, 40)
        esp.AlwaysOnTop = true
        esp.Parent = plr.Character.Head
        table.insert(espObjects, esp)

        local text = Instance.new("TextLabel")
        text.Size = UDim2.new(1, 0, 1, 0)
        text.BackgroundTransparency = 1
        text.Text = plr.Name .. " [" .. role .. "]"
        text.TextColor3 = color
        text.TextScaled = true
        text.Font = Enum.Font.GothamBold
        text.Parent = esp
        table.insert(espObjects, text)

        local highlight = Instance.new("Highlight")
        highlight.Name = "ESP_Highlight"
        highlight.Adornee = plr.Character
        highlight.FillColor = color
        highlight.FillTransparency = 0.5
        highlight.OutlineColor = color
        highlight.OutlineTransparency = 0.2
        highlight.Parent = plr.Character
        table.insert(espObjects, highlight)
    end
end

-- ===== TRIGGER BOT =====
local function toggleTriggerBot(state)
    toggles.triggerbot = state
    if state then
        if triggerbotConnection then triggerbotConnection:Disconnect() end
        triggerbotConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if not toggles.triggerbot then 
                if triggerbotConnection then triggerbotConnection:Disconnect() end
                return 
            end
            if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end

            local target, targetDist = nil, math.huge
            for _, plr in ipairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    if getPlayerRole(plr) == "Murderer" then
                        local dist = (plr.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                        if dist < targetDist and dist < 30 then
                            target, targetDist = plr, dist
                        end
                    end
                end
            end

            if target then
                player.Character.HumanoidRootPart.CFrame = CFrame.new(player.Character.HumanoidRootPart.Position, target.Character.HumanoidRootPart.Position)
                for _, tool in ipairs(player.Character:GetChildren()) do
                    if tool:IsA("Tool") then
                        tool:Activate()
                    end
                end
                task.wait(0.1)
                if target.Character and target.Character:FindFirstChild("Humanoid") and target.Character.Humanoid.Health > 0 then
                    target.Character.Humanoid.Health = 0
                end
            end
        end)
    else
        if triggerbotConnection then triggerbotConnection:Disconnect() end
    end
end

-- ===== SHOT MURDERER =====
local function toggleShotMurderer(state)
    toggles.shotMurderer = state
    if state then
        print("🔫 Режим убийства мардера включён! Нажмите H, чтобы убить мардера.")
    else
        print("🔫 Режим убийства мардера выключен.")
    end
end

local function killMurderer()
    if not toggles.shotMurderer then return end
    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            if getPlayerRole(plr) == "Murderer" then
                if plr.Character and plr.Character:FindFirstChild("Humanoid") then
                    plr.Character.Humanoid.Health = 0
                    print("🔫 Мардер убит!")
                    return
                end
            end
        end
    end
    print("❌ Мардер не найден!")
end

-- ===== BUNNY HOP =====
local function toggleBunnyHop(state)
    toggles.bunnyhop = state
    if state then
        if bunnyHopConnection then bunnyHopConnection:Disconnect() end
        bunnyHopConnection = game:GetService("RunService").Stepped:Connect(function()
            if not toggles.bunnyhop then return end
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                local humanoid = player.Character.Humanoid
                if humanoid:GetState() == Enum.HumanoidStateType.Jumping then
                    humanoid.WalkSpeed = settings.bunnyhop
                else
                    humanoid.WalkSpeed = 16
                end
            end
        end)
    else
        if bunnyHopConnection then bunnyHopConnection:Disconnect() end
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = 16
        end
    end
end

-- ===== SPIN BOT =====
local function toggleSpinBot(state)
    toggles.spinBot = state
    if state then
        if spinConnection then spinConnection:Disconnect() end
        spinConnection = game:GetService("RunService").Stepped:Connect(function()
            if not toggles.spinBot then 
                if spinConnection then spinConnection:Disconnect() end
                return 
            end
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                player.Character.HumanoidRootPart.CFrame = player.Character.HumanoidRootPart.CFrame * CFrame.Angles(0, math.rad(settings.spinSpeed), 0)
            end
        end)
    else
        if spinConnection then spinConnection:Disconnect() end
    end
end

-- ===== FLING =====
local function toggleFling(state)
    toggles.fling = state
    if state then
        if flingConnection then flingConnection:Disconnect() end
        flingConnection = game:GetService("RunService").Stepped:Connect(function()
            if not toggles.fling then 
                if flingConnection then flingConnection:Disconnect() end
                return 
            end
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                for _, plr in ipairs(game.Players:GetPlayers()) do
                    if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                        local dist = (plr.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                        if dist < 10 then
                            plr.Character.HumanoidRootPart.Velocity = Vector3.new(0, 1000, 0)
                        end
                    end
                end
            end
        end)
    else
        if flingConnection then flingConnection:Disconnect() end
    end
end

-- ===== БОМБА =====
local function toggleBombMode(state)
    toggles.bombMode = state
    if state then
        print("💣 Режим бомбы включён! Нажмите G, чтобы установить бомбу.")
    else
        print("💣 Режим бомбы выключен.")
        if bombPart then
            bombPart:Destroy()
            bombPart = nil
            bombActive = false
        end
    end
end

local function plantBomb()
    if not toggles.bombMode then return end
    if bombActive then
        print("⚠️ Бомба уже установлена!")
        return
    end

    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end

    bombPart = Instance.new("Part")
    bombPart.Name = "Bomb_MM2"
    bombPart.Size = Vector3.new(2, 1, 2)
    bombPart.BrickColor = BrickColor.new("Really red")
    bombPart.Material = Enum.Material.Neon
    bombPart.Position = character.HumanoidRootPart.Position + Vector3.new(0, 0, 5)
    bombPart.Anchored = true
    bombPart.CanCollide = false
    bombPart.Parent = workspace

    local glow = Instance.new("PointLight")
    glow.Color = Color3.fromRGB(255, 215, 0)
    glow.Range = 10
    glow.Brightness = 5
    glow.Parent = bombPart

    bombActive = true
    bombTimer = 5
    print("💣 Бомба установлена! Взрыв через 5 секунд! 🔥")

    spawn(function()
        while bombActive and bombTimer > 0 do
            wait(1)
            bombTimer = bombTimer - 1
            if bombTimer > 0 then
                print("⏱️ " .. bombTimer .. " секунд до взрыва!")
                if bombPart then
                    bombPart.BrickColor = bombTimer % 2 == 0 and BrickColor.new("Really red") or BrickColor.new("Bright red")
                end
            end
        end
        if bombActive and bombPart then
            print("💥 БОМБА ВЗОРВАЛАСЬ! 💥")
            local explosion = Instance.new("Explosion")
            explosion.Position = bombPart.Position
            explosion.BlastRadius = 25
            explosion.BlastDamage = 100
            explosion.Parent = workspace

            for i = 1, 20 do
                local spark = Instance.new("Part")
                spark.Size = Vector3.new(0.5, 0.5, 0.5)
                spark.BrickColor = BrickColor.new("Gold")
                spark.Material = Enum.Material.Neon
                spark.Position = bombPart.Position + Vector3.new(math.random(-10, 10), math.random(-5, 15), math.random(-10, 10))
                spark.Anchored = true
                spark.CanCollide = false
                spark.Parent = workspace
                spark.Touched:Connect(function(hit)
                    if hit.Parent and hit.Parent:FindFirstChild("Humanoid") then
                        hit.Parent.Humanoid.Health = 0
                    end
                end)
                game:GetService("Debris"):AddItem(spark, 2)
            end

            bombPart:Destroy()
            bombPart = nil
            bombActive = false
        end
    end)
end

local function defuseBomb()
    if not toggles.bombMode then return end
    if not bombActive or not bombPart then
        print("❌ Нет активной бомбы!")
        return
    end

    local character = player.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end

    local dist = (bombPart.Position - character.HumanoidRootPart.Position).Magnitude
    if dist > 10 then
        print("❌ Вы слишком далеко от бомбы!")
        return
    end

    print("🔧 Обезвреживание бомбы...")
    wait(2)

    if bombActive and bombPart then
        bombPart:Destroy()
        bombPart = nil
        bombActive = false
        print("✅ Бомба обезврежена!")
    end
end

-- Обработчики клавиш
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.G then
        if toggles.bombMode then
            if bombActive then
                defuseBomb()
            else
                plantBomb()
            end
        end
    end
    if input.KeyCode == Enum.KeyCode.H then
        if toggles.shotMurderer then
            killMurderer()
        end
    end
end)

-- ===== ОСТАЛЬНЫЕ ФУНКЦИИ =====
local function toggleJump(state)
    toggles.jump = state
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.JumpPower = state and settings.jumppower or 50
    end
end

local function enableNoclip()
    if noclipConnection then noclipConnection:Disconnect() end
    noclipActive = true
    noclipStartTime = tick()
    noclipConnection = game:GetService("RunService").Stepped:Connect(function()
        if not noclipActive then return end
        if tick() - noclipStartTime >= 86400 then
            disableNoclip()
            print("⏰ Noclip отключён (24 часа)")
            return
        end
        if player.Character then
            for _, part in ipairs(player.Character:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = false end
            end
        end
    end)
    player.CharacterAdded:Connect(function()
        if noclipActive then
            task.wait(0.5)
            if player.Character then
                for _, part in ipairs(player.Character:GetChildren()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end
    end)
end

local function disableNoclip()
    noclipActive = false
    if noclipConnection then noclipConnection:Disconnect() end
    if player.Character then
        for _, part in ipairs(player.Character:GetChildren()) do
            if part:IsA("BasePart") then part.CanCollide = true end
        end
    end
end

local function toggleNoclip(state)
    if state then enableNoclip() else disableNoclip() end
    toggles.noclip = state
end

local function toggleTeleport(state)
    toggles.teleport = state
    if state then
        mouse.Button1Down:Connect(function()
            if toggles.teleport and mouse.Target then
                local targetPos = mouse.Target.Position
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(targetPos)
                end
            end
        end)
    end
end

local function toggleFly(state)
    toggles.fly = state
    if state then
        if flyConnection then flyConnection:Disconnect() end
        flyConnection = game:GetService("RunService").Stepped:Connect(function()
            if not toggles.fly then 
                if flyConnection then flyConnection:Disconnect() end
                return 
            end
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local root = player.Character.HumanoidRootPart
                local moveDir = Vector3.new(0, 0, 0)
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + root.CFrame.LookVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - root.CFrame.LookVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - root.CFrame.RightVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + root.CFrame.RightVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end
                root.Velocity = moveDir * settings.flyspeed
            end
        end)
    else
        if flyConnection then flyConnection:Disconnect() end
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.PlatformStand = false
        end
    end
end

local function autoCollectGunFromSheriff()
    local gunFound, gunPos = nil, nil
    for _, item in ipairs(workspace:GetDescendants()) do
        if item:IsA("Tool") then
            local name = item.Name:lower()
            if name:find("gun") or name:find("pistol") or name:find("revolver") then
                if item.Parent ~= player.Character and item.Parent ~= player.Backpack then
                    local sheriffAlive = false
                    for _, plr in ipairs(game.Players:GetPlayers()) do
                        if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") and plr.Character.Humanoid.Health > 0 then
                            if getPlayerRole(plr) == "Sheriff" then sheriffAlive = true break end
                        end
                    end
                    if not sheriffAlive then
                        gunFound = item
                        gunPos = item.Position
                        break
                    end
                end
            end
        end
    end
    if gunFound and gunPos and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        player.Character.HumanoidRootPart.CFrame = CFrame.new(gunPos + Vector3.new(0, 2, 0))
        task.wait(0.3)
        gunFound.Parent = player.Character
        print("🔫 Оружие шерифа подобрано!")
        return true
    end
    return false
end

local function toggleAutoCollectGun(state)
    toggles.autoCollectGun = state
    if state then
        game:GetService("RunService").Heartbeat:Connect(function()
            if toggles.autoCollectGun then autoCollectGunFromSheriff() end
        end)
    end
end

local function toggleFps(state)
    toggles.showFps = state
    if state then
        if fpsLabel then fpsLabel:Destroy() end
        local screenGui = Instance.new("ScreenGui")
        screenGui.Name = "FPSDisplay"
        screenGui.Parent = game.CoreGui
        fpsLabel = Instance.new("TextLabel")
        fpsLabel.Size = UDim2.new(0, 100, 0, 30)
        fpsLabel.Position = UDim2.new(0, 10, 0, 10)
        fpsLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        fpsLabel.BackgroundTransparency = 0.3
        fpsLabel.BorderSizePixel = 0
        fpsLabel.Text = "FPS: 0"
        fpsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        fpsLabel.TextScaled = true
        fpsLabel.Font = Enum.Font.GothamBold
        fpsLabel.Parent = screenGui
        local frames = 0
        local lastTime = tick()
        game:GetService("RunService").Heartbeat:Connect(function()
            frames = frames + 1
            if tick() - lastTime >= 1 then
                if fpsLabel then fpsLabel.Text = "FPS: " .. frames end
                frames = 0
                lastTime = tick()
            end
        end)
    else
        if fpsLabel then fpsLabel:Destroy() end
    end
end

-- ===== ГЛАВНОЕ МЕНЮ =====
local function createRyzenHub()
    if game.CoreGui:FindFirstChild("RyzenHubGUI") then
        game.CoreGui.RyzenHubGUI:Destroy()
    end

    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "RyzenHubGUI"
    screenGui.Parent = game.CoreGui
    screenGui.ResetOnSpawn = false

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 450, 0, 650)
    mainFrame.Position = UDim2.new(0.5, -225, 0.5, -325)
    mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    mainFrame.BackgroundTransparency = 0.15
    mainFrame.BorderSizePixel = 0
    mainFrame.Active = true
    mainFrame.Draggable = true
    mainFrame.Parent = screenGui

    -- Заголовок
    local titleFrame = Instance.new("Frame")
    titleFrame.Size = UDim2.new(1, 0, 0, 45)
    titleFrame.Position = UDim2.new(0, 0, 0, 0)
    titleFrame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
    titleFrame.BorderSizePixel = 0
    titleFrame.Parent = mainFrame

    local logo = Instance.new("ImageLabel")
    logo.Size = UDim2.new(0, 35, 0, 35)
    logo.Position = UDim2.new(0.02, 0, 0.1, 0)
    logo.BackgroundTransparency = 1
    logo.Image = "https://upload.wikimedia.org/wikipedia/commons/thumb/7/7f/AMD_Ryzen_logo.svg/120px-AMD_Ryzen_logo.svg.png"
    logo.Parent = titleFrame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -50, 1, 0)
    title.Position = UDim2.new(0, 50, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "RYZEN HUB"
    title.TextColor3 = Color3.fromRGB(255, 80, 80)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = titleFrame

    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -36, 0, 8)
    closeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
    closeBtn.BorderSizePixel = 0
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    closeBtn.TextScaled = true
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = titleFrame
    closeBtn.MouseButton1Click:Connect(function() screenGui:Destroy() end)

    local function createButton(text, yPos, callback)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.85, 0, 0, 30)
        btn.Position = UDim2.new(0.075, 0, yPos, 0)
        btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
        btn.BorderSizePixel = 0
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.TextScaled = true
        btn.Font = Enum.Font.Gotham
        btn.Parent = mainFrame

        local isActive = false
        btn.MouseButton1Click:Connect(function()
            isActive = not isActive
            if isActive then
                btn.BackgroundColor3 = Color3.fromRGB(75, 75, 80)
                btn.TextColor3 = Color3.fromRGB(255, 120, 120)
            else
                btn.BackgroundColor3 = Color3.fromRGB(50, 50, 55)
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
            callback(isActive)
            if text:find("ESP") then updateESP() end
        end)
        return btn
    end

    local function createSlider(text, yPos, minVal, maxVal, defaultVal, callback)
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(0.85, 0, 0, 30)
        frame.Position = UDim2.new(0.075, 0, yPos, 0)
        frame.BackgroundColor3 = Color3.fromRGB(45, 45, 50)
        frame.BorderSizePixel = 0
        frame.Parent = mainFrame

        local label = Instance.new("TextLabel")
        label.Size = UDim2.new(0.4, 0, 1, 0)
        label.Position = UDim2.new(0.02, 0, 0, 0)
        label.BackgroundTransparency = 1
        label.Text = text
        label.TextColor3 = Color3.fromRGB(255, 255, 255)
        label.TextScaled = true
        label.Font = Enum.Font.Gotham
        label.Parent = frame

        local slider = Instance.new("TextButton")
        slider.Size = UDim2.new(0.4, 0, 0.6, 0)
        slider.Position = UDim2.new(0.45, 0, 0.2, 0)
        slider.BackgroundColor3 = Color3.fromRGB(60, 60, 65)
        slider.BorderSizePixel = 0
        slider.Text = tostring(defaultVal)
        slider.TextColor3 = Color3.fromRGB(255, 255, 255)
        slider.TextScaled = true
        slider.Font = Enum.Font.Gotham
        slider.Parent = frame

        local value = defaultVal
        slider.MouseButton1Click:Connect(function()
            local newVal = value + 10
            if newVal > maxVal then newVal = minVal end
            value = newVal
            slider.Text = tostring(value)
            callback(value)
        end)
        return slider
    end

    -- КНОПКИ
    createButton("👁️ ESP (ВКЛ)", 0.10, function(state)
        toggles.esp = state
        updateESP()
    end)
    createButton("🔴 Murderer", 0.17, function(state) 
        toggles.showMurderer = state 
    end)
    createButton("🟢 Sheriff", 0.24, function(state) 
        toggles.showSheriff = state 
    end)
    createButton("⚪ Innocent", 0.31, function(state) 
        toggles.showInnocent = state 
    end)
    createButton("🎯 Trigger Bot", 0.38, toggleTriggerBot)
    createButton("🐇 Bunny Hop", 0.45, toggleBunnyHop)
    createButton("🦘 Super Jump", 0.52, toggleJump)
    createButton("🌀 Noclip", 0.59, toggleNoclip)
    createButton("📍 Teleport", 0.66, toggleTeleport)
    createButton("✈️ Fly", 0.73, toggleFly)
    createButton("🔫 Забрать оружие", 0.80, toggleAutoCollectGun)
    createButton("💣 Bomb Mode [G]", 0.87, toggleBombMode)
    createButton("🔫 Shot Murderer [H]", 0.94, toggleShotMurderer)
    createButton("🍆 Дрочка", 1.01, toggleMasturbate)
    createButton("👥 Список игроков", 1.08, function()
        togglePlayerList()
    end)

    -- СЛАЙДЕРЫ
    createSlider("🐇 Bunny Hop Speed", 1.15, 20, 300, settings.bunnyhop, function(val)
        settings.bunnyhop = val
    end)
    createSlider("✈️ Fly Speed", 1.22, 10, 200, settings.flyspeed, function(val)
        settings.flyspeed = val
    end)
    createSlider("🦘 Jump Power", 1.29, 50, 300, settings.jumppower, function(val)
        settings.jumppower = val
    end)

    print("✅ Ryzen Hub загружен!")
end

-- ===== ОТКРЫТИЕ ПО ПРАВОМУ SHIFT =====
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightShift then
        if game.CoreGui:FindFirstChild("RyzenHubGUI") then
            game.CoreGui.RyzenHubGUI:Destroy()
        else
            createRyzenHub()
        end
    end
end)

createRyzenHub()