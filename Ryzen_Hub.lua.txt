-- KABANCIK HUB - ПОЛНАЯ ВЕРСИЯ (РАБОТАЕТ БЕЗ HTTP)
local player = game.Players.LocalPlayer
local mouse = player:GetMouse()
local uis = game:GetService("UserInputService")
local rs = game:GetService("RunService")
local lighting = game:GetService("Lighting")
local workspace = game:GetService("Workspace")

-- ===== НАСТРОЙКИ =====
local settings = {
    speedGlitch = 100,
    wallHopSpeed = 200,
    hitboxSize = 5,
    flingPower = 1000,
    silentAimTarget = "All",
    autoCollectRange = 30,
    flingTarget = "All",
    banType = "Kick",
    espMode = "Имена",
    espTransparency = 0.5,
    silentAimRange = 150,
    silentAimPart = "Голова",
    silentAimAutoShoot = false,
    silentAimLock = false,
    invisibleTransparency = 0.1,
}

local toggles = {
    esp = false,
    silentAim = false,
    hitbox = false,
    antiAFK = false,
    autoCollect = false,
    killAll = false,
    wallHop = false,
    speedGlitch = false,
    teleportToPlayer = false,
    flingPlayer = false,
    banPlayer = false,
    invisible = false,
    animeSky = false,
    fog = false,
    atmosphere = false,
    bloom = false,
    colorCorrection = false,
}

local gui = nil
local miniMenu = nil
local espObjects = {}
local silentAimConnection = nil
local visualEffects = {}

-- ===== ОПРЕДЕЛЕНИЕ РОЛИ =====
local function getRole(plr)
    if not plr or not plr.Character then return "Innocent" end
    for _, tool in ipairs(plr.Character:GetChildren()) do
        if tool:IsA("Tool") then
            local n = tool.Name:lower()
            if n:find("knife") or n:find("dagger") then return "Murderer" end
            if n:find("gun") or n:find("pistol") or n:find("revolver") then return "Sheriff" end
        end
    end
    if plr.Backpack then
        for _, tool in ipairs(plr.Backpack:GetChildren()) do
            local n = tool.Name:lower()
            if n:find("knife") or n:find("dagger") then return "Murderer" end
            if n:find("gun") or n:find("pistol") or n:find("revolver") then return "Sheriff" end
        end
    end
    return "Innocent"
end

-- ===== INVISIBLE =====
local function toggleInvisible(state)
    toggles.invisible = state
    local character = player.Character
    if not character then return end
    local transparency = state and settings.invisibleTransparency or 0
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") or part:IsA("MeshPart") or part:IsA("Part") then
            part.Transparency = transparency
        end
    end
    for _, accessory in ipairs(character:GetChildren()) do
        if accessory:IsA("Accessory") then
            for _, part in ipairs(accessory:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("MeshPart") or part:IsA("Part") then
                    part.Transparency = transparency
                end
            end
        end
    end
end

-- ===== ESP =====
function updateESP()
    for _, v in pairs(espObjects) do v:Destroy() end
    espObjects = {}
    if not toggles.esp then return end
    
    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
            local role = getRole(plr)
            local color = Color3.fromRGB(0, 255, 0)
            if role == "Murderer" then color = Color3.fromRGB(255, 0, 0)
            elseif role == "Sheriff" then color = Color3.fromRGB(0, 100, 255) end

            local esp = Instance.new("BillboardGui")
            esp.Size = UDim2.new(0, 200, 0, 50)
            esp.AlwaysOnTop = true
            esp.Parent = plr.Character.HumanoidRootPart

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.TextColor3 = color
            label.TextScaled = true
            label.Font = Enum.Font.GothamBold
            
            if settings.espMode == "Имена" then
                label.Text = plr.Name
            elseif settings.espMode == "Роли" then
                label.Text = role
            else
                local dist = (plr.Character.HumanoidRootPart.Position - player.Character.HumanoidRootPart.Position).Magnitude
                label.Text = math.floor(dist) .. "м"
            end
            label.Parent = esp

            table.insert(espObjects, esp)
        end
    end
end

-- ===== ФУНКЦИИ =====
function toggleSilentAim(s) toggles.silentAim = s end
function toggleHitbox(s) toggles.hitbox = s end
function toggleAntiAFK(s) toggles.antiAFK = s end
function toggleAutoCollect(s) toggles.autoCollect = s end
function toggleKillAll(s) toggles.killAll = s end
function toggleWallHop(s) toggles.wallHop = s end
function toggleSpeedGlitch(s) toggles.speedGlitch = s end
function toggleTeleportToPlayer(s) toggles.teleportToPlayer = s end
function toggleFlingPlayer(s) toggles.flingPlayer = s end
function toggleBanPlayer(s) toggles.banPlayer = s end

-- ===== МИНИ-МЕНЮ =====
local function createMiniMenu(funcName, x, y)
    if miniMenu then miniMenu:Destroy() end
    miniMenu = Instance.new("ScreenGui")
    miniMenu.Name = "MiniMenu"
    miniMenu.Parent = game.CoreGui

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 280, 0, 250)
    frame.Position = UDim2.new(0, x, 0, y)
    frame.BackgroundColor3 = Color3.fromRGB(20, 0, 0)
    frame.BackgroundTransparency = 0.1
    frame.BorderSizePixel = 2
    frame.BorderColor3 = Color3.fromRGB(255, 0, 0)
    frame.Active = true
    frame.Draggable = true
    frame.Parent = miniMenu

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1,0,0,30)
    title.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
    title.BorderSizePixel = 0
    title.Text = "⚙️ " .. funcName
    title.TextColor3 = Color3.fromRGB(255, 50, 50)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = frame

    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0,25,0,25)
    close.Position = UDim2.new(1,-30,0,2)
    close.BackgroundColor3 = Color3.fromRGB(80,0,0)
    close.BorderSizePixel = 0
    close.Text = "✕"
    close.TextColor3 = Color3.fromRGB(255,255,255)
    close.TextScaled = true
    close.Font = Enum.Font.GothamBold
    close.Parent = frame
    close.MouseButton1Click:Connect(function() miniMenu:Destroy() end)

    local function createSlider(txt, y, min, max, def, cb)
        local fr = Instance.new("Frame")
        fr.Size = UDim2.new(0.9,0,0,25)
        fr.Position = UDim2.new(0.05,0,y,0)
        fr.BackgroundColor3 = Color3.fromRGB(30,0,0)
        fr.BorderSizePixel = 0
        fr.Parent = frame

        local lbl = Instance.new("TextLabel")
        lbl.Size = UDim2.new(0.4,0,1,0)
        lbl.Position = UDim2.new(0.02,0,0,0)
        lbl.BackgroundTransparency = 1
        lbl.Text = txt
        lbl.TextColor3 = Color3.fromRGB(255,200,200)
        lbl.TextScaled = true
        lbl.Font = Enum.Font.Gotham
        lbl.Parent = fr

        local sl = Instance.new("TextButton")
        sl.Size = UDim2.new(0.4,0,0.7,0)
        sl.Position = UDim2.new(0.45,0,0.15,0)
        sl.BackgroundColor3 = Color3.fromRGB(50,0,0)
        sl.BorderSizePixel = 1
        sl.BorderColor3 = Color3.fromRGB(255,50,50)
        sl.Text = tostring(def)
        sl.TextColor3 = Color3.fromRGB(255,255,255)
        sl.TextScaled = true
        sl.Font = Enum.Font.Gotham
        sl.Parent = fr
        local val = def
        sl.MouseButton1Click:Connect(function()
            val = val + 1
            if val > max then val = min end
            sl.Text = tostring(val)
            cb(val)
        end)
    end

    local function createBtn(text, y, cb)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.8,0,0,25)
        btn.Position = UDim2.new(0.1,0,y,0)
        btn.BackgroundColor3 = Color3.fromRGB(40,0,0)
        btn.BorderSizePixel = 1
        btn.BorderColor3 = Color3.fromRGB(255,50,50)
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255,255,255)
        btn.TextScaled = true
        btn.Font = Enum.Font.Gotham
        btn.Parent = frame
        btn.MouseButton1Click:Connect(cb)
        return btn
    end

    local yPos = 0.12

    if funcName == "Invisible" then
        createSlider("Прозрачность", yPos, 0, 10, settings.invisibleTransparency * 10, function(v) 
            settings.invisibleTransparency = v / 10
            if toggles.invisible then toggleInvisible(true) end
        end)
    elseif funcName == "Silent Aim" then
        createBtn("Цель: " .. settings.silentAimTarget, yPos, function()
            local targets = {"All", "Murderer", "Sheriff", "Innocent"}
            local idx = table.find(targets, settings.silentAimTarget) or 1
            settings.silentAimTarget = targets[idx % #targets + 1]
            btn.Text = "Цель: " .. settings.silentAimTarget
        end)
        yPos = yPos + 0.1
        createSlider("Радиус", yPos, 50, 500, settings.silentAimRange, function(v) settings.silentAimRange = v end)
    elseif funcName == "ESP" then
        createBtn("Режим: " .. settings.espMode, yPos, function()
            local modes = {"Имена", "Роли", "Расстояние"}
            local idx = table.find(modes, settings.espMode) or 1
            settings.espMode = modes[idx % #modes + 1]
            btn.Text = "Режим: " .. settings.espMode
            updateESP()
        end)
        yPos = yPos + 0.1
        createSlider("Прозрачность", yPos, 1, 10, settings.espTransparency * 10, function(v) 
            settings.espTransparency = v / 10
            updateESP()
        end)
    elseif funcName == "Hitbox" then
        createSlider("Размер", yPos, 1, 20, settings.hitboxSize, function(v) 
            settings.hitboxSize = v
            if toggles.hitbox then
                for _, plr in ipairs(game.Players:GetPlayers()) do
                    if plr ~= player and plr.Character then
                        for _, part in ipairs(plr.Character:GetChildren()) do
                            if part:IsA("BasePart") then
                                part.Size = Vector3.new(v, v, v)
                            end
                        end
                    end
                end
            end
        end)
    elseif funcName == "Wall Hop" then
        createSlider("Скорость", yPos, 50, 500, settings.wallHopSpeed, function(v) settings.wallHopSpeed = v end)
    elseif funcName == "Speed Glitch" then
        createSlider("Скорость", yPos, 20, 300, settings.speedGlitch, function(v) settings.speedGlitch = v end)
    elseif funcName == "Auto Collect" then
        createSlider("Радиус", yPos, 10, 100, settings.autoCollectRange, function(v) settings.autoCollectRange = v end)
    elseif funcName == "Fling" then
        createSlider("Сила", yPos, 100, 5000, settings.flingPower, function(v) settings.flingPower = v end)
    end
end

-- ===== ГЛАВНОЕ МЕНЮ =====
local function createMenu()
    if gui then gui:Destroy() end
    gui = Instance.new("ScreenGui")
    gui.Name = "KabancikHubGUI"
    gui.Parent = game.CoreGui
    gui.ResetOnSpawn = false

    local main = Instance.new("Frame")
    main.Size = UDim2.new(0, 350, 0, 480)
    main.Position = UDim2.new(0.5, -175, 0.5, -240)
    main.BackgroundColor3 = Color3.fromRGB(20, 0, 0)
    main.BackgroundTransparency = 0.1
    main.BorderSizePixel = 2
    main.BorderColor3 = Color3.fromRGB(255, 0, 0)
    main.Active = true
    main.Draggable = true
    main.Parent = gui

    local title = Instance.new("Frame")
    title.Size = UDim2.new(1,0,0,40)
    title.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
    title.BorderSizePixel = 0
    title.Parent = main

    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.Text = "✦ KABANCIK HUB ✦"
    txt.TextColor3 = Color3.fromRGB(255, 50, 50)
    txt.TextScaled = true
    txt.Font = Enum.Font.GothamBold
    txt.Parent = title

    local close = Instance.new("TextButton")
    close.Size = UDim2.new(0, 30, 0, 30)
    close.Position = UDim2.new(1, -38, 0, 5)
    close.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
    close.BorderSizePixel = 0
    close.Text = "✕"
    close.TextColor3 = Color3.fromRGB(255, 255, 255)
    close.TextScaled = true
    close.Font = Enum.Font.GothamBold
    close.Parent = title
    close.MouseButton1Click:Connect(function() gui:Destroy() end)

    local function createBtn(text, y, func)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.85, 0, 0, 30)
        btn.Position = UDim2.new(0.075, 0, y, 0)
        btn.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
        btn.BorderSizePixel = 1
        btn.BorderColor3 = Color3.fromRGB(255, 50, 50)
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.TextScaled = true
        btn.Font = Enum.Font.Gotham
        btn.Parent = main
        
        local active = false
        btn.MouseButton1Click:Connect(function()
            active = not active
            if active then
                btn.BackgroundColor3 = Color3.fromRGB(100, 0, 0)
                btn.TextColor3 = Color3.fromRGB(255, 150, 150)
            else
                btn.BackgroundColor3 = Color3.fromRGB(40, 0, 0)
                btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            end
            func(active)
            if text:find("ESP") then updateESP() end
        end)
        
        btn.MouseButton2Click:Connect(function()
            local name = text:gsub("👁️ ", ""):gsub("🎯 ", ""):gsub("📦 ", ""):gsub("💤 ", ""):gsub("💰 ", ""):gsub("💀 ", ""):gsub("🧱 ", ""):gsub("⚡ ", ""):gsub("👤 ", ""):gsub("🌀 ", ""):gsub("🚫 ", ""):gsub("👻 ", "")
            createMiniMenu(name, mouse.X, mouse.Y)
        end)
        return btn
    end

    createBtn("👁️ ESP", 0.08, function(s) toggles.esp = s updateESP() end)
    createBtn("🎯 Silent Aim", 0.16, toggleSilentAim)
    createBtn("📦 Hitbox", 0.24, toggleHitbox)
    createBtn("💤 Anti-AFK", 0.32, toggleAntiAFK)
    createBtn("💰 Auto Collect", 0.40, toggleAutoCollect)
    createBtn("💀 Kill All", 0.48, toggleKillAll)
    createBtn("🧱 Wall Hop", 0.56, toggleWallHop)
    createBtn("⚡ Speed Glitch", 0.64, toggleSpeedGlitch)
    createBtn("👤 Teleport", 0.72, toggleTeleportToPlayer)
    createBtn("🌀 Fling", 0.80, toggleFlingPlayer)
    createBtn("🚫 Ban", 0.88, toggleBanPlayer)
    createBtn("👻 Invisible", 0.96, toggleInvisible)

    print("✅ KABANCIK HUB загружен!")
end

-- ===== ОТКРЫТИЕ ПО ПРАВОМУ SHIFT =====
uis.InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightShift then
        if gui and gui.Parent then
            gui:Destroy()
            gui = nil
        else
            createMenu()
        end
    end
end)

-- Автоматическое открытие
createMenu()
print("✅ KABANCIK HUB готов! ЛКМ - включить, ПКМ - настройки")