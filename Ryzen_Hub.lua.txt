-- RYZEN HUB - СТИЛЬ МАУС (ДЛЯ MM2)
local player = game.Players.LocalPlayer
local mouse = player:GetMouse()

-- ===== НАСТРОЙКИ =====
local settings = {
    espEnabled = false,
    espMode = "Роли", -- Роли / Имена / Все
    espColor = Color3.fromRGB(255, 0, 0),
    espTransparency = 0.5,
    espNames = true,
    espHealth = true,
    aimbotEnabled = false,
    autoShoot = false,
    fovCircle = false,
    fovRadius = 150,
    smoothness = 5,
    aimPart = "Head",
    fovColor = Color3.fromRGB(255, 255, 255),
    speed = 60,
    jumpPower = 120,
    flySpeed = 50,
    noclip = false,
    teleport = false,
    fly = false,
    triggerbot = false,
    shotMurderer = false,
    autoCollectGun = false,
}

local gui = nil
local espObjects = {}
local fpsLabel = nil
local selectedTab = "ESP"

-- ===== ОПРЕДЕЛЕНИЕ РОЛИ =====
local function getPlayerRole(plr)
    if not plr or not plr.Character then return "Innocent" end
    for _, tool in ipairs(plr.Character:GetChildren()) do
        if tool:IsA("Tool") then
            local name = tool.Name:lower()
            if name:find("knife") or name:find("dagger") then return "Murderer" end
            if name:find("gun") or name:find("pistol") or name:find("revolver") then return "Sheriff" end
        end
    end
    if plr.Backpack then
        for _, tool in ipairs(plr.Backpack:GetChildren()) do
            if tool:IsA("Tool") then
                local name = tool.Name:lower()
                if name:find("knife") or name:find("dagger") then return "Murderer" end
                if name:find("gun") or name:find("pistol") or name:find("revolver") then return "Sheriff" end
            end
        end
    end
    return "Innocent"
end

-- ===== ESP =====
local function updateESP()
    for _, obj in ipairs(espObjects) do
        pcall(function() obj:Destroy() end)
    end
    espObjects = {}
    if not settings.espEnabled then return end

    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr == player then continue end
        if not plr.Character or not plr.Character:FindFirstChild("Head") then continue end
        
        local role = getPlayerRole(plr)
        local color = role == "Murderer" and Color3.fromRGB(255, 0, 0) or 
                     role == "Sheriff" and Color3.fromRGB(0, 100, 255) or 
                     Color3.fromRGB(0, 255, 0)
        
        if settings.espColor then
            color = settings.espColor
        end

        local esp = Instance.new("BillboardGui")
        esp.Name = "ESP_Ryzen"
        esp.Size = UDim2.new(0, 150, 0, 40)
        esp.AlwaysOnTop = true
        esp.Parent = plr.Character.Head
        table.insert(espObjects, esp)

        local text = Instance.new("TextLabel")
        text.Size = UDim2.new(1, 0, 1, 0)
        text.BackgroundTransparency = 1
        if settings.espMode == "Роли" then
            text.Text = plr.Name .. " [" .. role .. "]"
        elseif settings.espMode == "Имена" then
            text.Text = plr.Name
        else
            text.Text = plr.Name .. " [" .. role .. "]"
        end
        text.TextColor3 = color
        text.TextScaled = true
        text.Font = Enum.Font.GothamBold
        text.Parent = esp
        table.insert(espObjects, text)

        if settings.espHealth and plr.Character:FindFirstChild("Humanoid") then
            local health = Instance.new("TextLabel")
            health.Size = UDim2.new(1, 0, 0, 15)
            health.Position = UDim2.new(0, 0, 1, 0)
            health.BackgroundTransparency = 1
            health.Text = "❤️ " .. math.round(plr.Character.Humanoid.Health)
            health.TextColor3 = Color3.fromRGB(255, 255, 255)
            health.TextScaled = true
            health.Font = Enum.Font.Gotham
            health.Parent = esp
            table.insert(espObjects, health)
        end

        local highlight = Instance.new("Highlight")
        highlight.Name = "ESP_Highlight"
        highlight.Adornee = plr.Character
        highlight.FillColor = color
        highlight.FillTransparency = settings.espTransparency or 0.5
        highlight.OutlineColor = color
        highlight.OutlineTransparency = 0.2
        highlight.Parent = plr.Character
        table.insert(espObjects, highlight)
    end
end

-- ===== AIMBOT =====
local aimbotConnection = nil
local function toggleAimbot(state)
    settings.aimbotEnabled = state
    if state then
        if aimbotConnection then aimbotConnection:Disconnect() end
        aimbotConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if not settings.aimbotEnabled then 
                if aimbotConnection then aimbotConnection:Disconnect() end
                return 
            end
            if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end
            
            local target, targetDist = nil, math.huge
            for _, plr in ipairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Character and plr.Character:FindFirstChild(settings.aimPart) then
                    local dist = (plr.Character[settings.aimPart].Position - player.Character.HumanoidRootPart.Position).Magnitude
                    if dist < targetDist and dist < settings.fovRadius then
                        target = plr
                        targetDist = dist
                    end
                end
            end
            
            if target then
                local aimPart = target.Character[settings.aimPart]
                player.Character.HumanoidRootPart.CFrame = CFrame.new(player.Character.HumanoidRootPart.Position, aimPart.Position)
                
                if settings.autoShoot then
                    for _, tool in ipairs(player.Character:GetChildren()) do
                        if tool:IsA("Tool") then
                            tool:Activate()
                        end
                    end
                end
            end
        end)
    else
        if aimbotConnection then aimbotConnection:Disconnect() end
    end
end

-- ===== FOV КРУГ =====
local fovCircle = nil
local function updateFOV()
    if fovCircle then fovCircle:Destroy() end
    if not settings.fovCircle then return end
    
    fovCircle = Instance.new("Frame")
    fovCircle.Size = UDim2.new(0, settings.fovRadius * 2, 0, settings.fovRadius * 2)
    fovCircle.Position = UDim2.new(0.5, -settings.fovRadius, 0.5, -settings.fovRadius)
    fovCircle.BackgroundColor3 = settings.fovColor
    fovCircle.BackgroundTransparency = 0.8
    fovCircle.BorderSizePixel = 1
    fovCircle.BorderColor3 = settings.fovColor
    fovCircle.Active = false
    fovCircle.Parent = game.CoreGui
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(1, 0)
    corner.Parent = fovCircle
end

-- ===== TRIGGER BOT =====
local triggerbotConnection = nil
local function toggleTriggerBot(state)
    settings.triggerbot = state
    if state then
        if triggerbotConnection then triggerbotConnection:Disconnect() end
        triggerbotConnection = game:GetService("RunService").Heartbeat:Connect(function()
            if not settings.triggerbot then 
                if triggerbotConnection then triggerbotConnection:Disconnect() end
                return 
            end
            for _, plr in ipairs(game.Players:GetPlayers()) do
                if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") then
                    if getPlayerRole(plr) == "Murderer" then
                        pcall(function()
                            plr.Character.Humanoid.Health = 0
                        end)
                    end
                end
            end
        end)
    else
        if triggerbotConnection then triggerbotConnection:Disconnect() end
    end
end

-- ===== SHOT MURDERER =====
local function toggleShotMurderer(state)
    settings.shotMurderer = state
end

local function killMurderer()
    if not settings.shotMurderer then return end
    for _, plr in ipairs(game.Players:GetPlayers()) do
        if plr ~= player and plr.Character and plr.Character:FindFirstChild("Humanoid") then
            if getPlayerRole(plr) == "Murderer" then
                pcall(function() plr.Character.Humanoid.Health = 0 end)
                return
            end
        end
    end
end

-- ===== BUNNY HOP =====
local bunnyHopConnection = nil
local function toggleBunnyHop(state)
    settings.bunnyhop = state
    if state then
        if bunnyHopConnection then bunnyHopConnection:Disconnect() end
        bunnyHopConnection = game:GetService("RunService").Stepped:Connect(function()
            if not settings.bunnyhop then return end
            if player.Character and player.Character:FindFirstChild("Humanoid") then
                local humanoid = player.Character.Humanoid
                if humanoid:GetState() == Enum.HumanoidStateType.Jumping then
                    humanoid.WalkSpeed = settings.speed
                else
                    humanoid.WalkSpeed = 16
                end
            end
        end)
    else
        if bunnyHopConnection then bunnyHopConnection:Disconnect() end
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.WalkSpeed = 16
        end
    end
end

-- ===== SPEED =====
local function toggleSpeed(state)
    settings.speedEnabled = state
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.WalkSpeed = state and settings.speed or 16
    end
end

-- ===== SUPER JUMP =====
local function toggleJump(state)
    settings.jumpEnabled = state
    if player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.JumpPower = state and settings.jumpPower or 50
    end
end

-- ===== FLY =====
local flyConnection = nil
local function toggleFly(state)
    settings.fly = state
    if state then
        if flyConnection then flyConnection:Disconnect() end
        flyConnection = game:GetService("RunService").Stepped:Connect(function()
            if not settings.fly then 
                if flyConnection then flyConnection:Disconnect() end
                return 
            end
            if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local root = player.Character.HumanoidRootPart
                local moveDir = Vector3.new(0, 0, 0)
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + root.CFrame.LookVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - root.CFrame.LookVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - root.CFrame.RightVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + root.CFrame.RightVector end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.Space) then moveDir = moveDir + Vector3.new(0, 1, 0) end
                if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.LeftShift) then moveDir = moveDir - Vector3.new(0, 1, 0) end
                root.Velocity = moveDir * settings.flySpeed
            end
        end)
    else
        if flyConnection then flyConnection:Disconnect() end
        if player.Character and player.Character:FindFirstChild("Humanoid") then
            player.Character.Humanoid.PlatformStand = false
        end
    end
end

-- ===== NOCLIP =====
local noclipConnection = nil
local function toggleNoclip(state)
    settings.noclip = state
    if state then
        if noclipConnection then noclipConnection:Disconnect() end
        noclipConnection = game:GetService("RunService").Stepped:Connect(function()
            if not settings.noclip then 
                if noclipConnection then noclipConnection:Disconnect() end
                return 
            end
            if player.Character then
                for _, part in ipairs(player.Character:GetChildren()) do
                    if part:IsA("BasePart") then part.CanCollide = false end
                end
            end
        end)
    else
        if noclipConnection then noclipConnection:Disconnect() end
        if player.Character then
            for _, part in ipairs(player.Character:GetChildren()) do
                if part:IsA("BasePart") then part.CanCollide = true end
            end
        end
    end
end

-- ===== TELEPORT =====
local function toggleTeleport(state)
    settings.teleport = state
    if state then
        mouse.Button1Down:Connect(function()
            if settings.teleport and mouse.Target then
                local targetPos = mouse.Target.Position
                if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                    player.Character.HumanoidRootPart.CFrame = CFrame.new(targetPos)
                end
            end
        end)
    end
end

-- ===== АВТО-ПОДБОР ПИСТОЛЕТА =====
local function toggleAutoCollectGun(state)
    settings.autoCollectGun = state
end

local function autoCollectGun()
    if not settings.autoCollectGun then return end
    if not player.Character or not player.Character:FindFirstChild("HumanoidRootPart") then return end
    local root = player.Character.HumanoidRootPart
    for _, item in ipairs(workspace:GetDescendants()) do
        if item:IsA("Tool") then
            local name = item.Name:lower()
            if name:find("gun") or name:find("pistol") or name:find("revolver") then
                if item.Parent ~= player.Character and item.Parent ~= player.Backpack then
                    local dist = (item.Position - root.Position).Magnitude
                    if dist < 100 then
                        pcall(function()
                            root.CFrame = CFrame.new(item.Position + Vector3.new(0, 2, 0))
                            task.wait(0.2)
                            item.Parent = player.Character
                        end)
                        return
                    end
                end
            end
        end
    end
end

player.CharacterAdded:Connect(autoCollectGun)

local function pickupGun()
    autoCollectGun()
end

-- ===== FPS =====
local function toggleFps(state)
    settings.showFps = state
    if state then
        if fpsLabel then fpsLabel:Destroy() end
        local screenGui = Instance.new("ScreenGui")
        screenGui.Name = "FPSDisplay"
        screenGui.Parent = game.CoreGui
        fpsLabel = Instance.new("TextLabel")
        fpsLabel.Size = UDim2.new(0, 100, 0, 30)
        fpsLabel.Position = UDim2.new(0, 10, 0, 10)
        fpsLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        fpsLabel.BackgroundTransparency = 0.3
        fpsLabel.BorderSizePixel = 0
        fpsLabel.Text = "FPS: 0"
        fpsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        fpsLabel.TextScaled = true
        fpsLabel.Font = Enum.Font.GothamBold
        fpsLabel.Parent = screenGui
        local frames = 0
        local lastTime = tick()
        game:GetService("RunService").Heartbeat:Connect(function()
            frames = frames + 1
            if tick() - lastTime >= 1 then
                if fpsLabel then fpsLabel.Text = "FPS: " .. frames end
                frames = 0
                lastTime = tick()
            end
        end)
    else
        if fpsLabel then fpsLabel:Destroy() end
    end
end

-- ===== ГЛАВНОЕ МЕНЮ (СТИЛЬ МАУС) =====
local function createRyzenHub()
    if gui then gui:Destroy() end

    gui = Instance.new("ScreenGui")
    gui.Name = "RyzenHubGUI"
    gui.Parent = game.CoreGui
    gui.ResetOnSpawn = false

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 650, 0, 500)
    mainFrame.Position = UDim2.new(0.5, -325, 0.5, -250)
    mainFrame.BackgroundColor3 = Color3.fromRGB(15, 0, 15)
    mainFrame.BackgroundTransparency = 0.1
    mainFrame.BorderSizePixel = 2
    mainFrame.BorderColor3 = Color3.fromRGB(200, 0, 255)
    mainFrame.Active = true
    mainFrame.Draggable = true
    mainFrame.Parent = gui

    -- ЛЕВАЯ ПАНЕЛЬ (КНОПКИ)
    local leftPanel = Instance.new("Frame")
    leftPanel.Size = UDim2.new(0, 200, 1, 0)
    leftPanel.BackgroundColor3 = Color3.fromRGB(25, 0, 25)
    leftPanel.BorderSizePixel = 0
    leftPanel.Parent = mainFrame

    local logo = Instance.new("ImageLabel")
    logo.Size = UDim2.new(0, 30, 0, 30)
    logo.Position = UDim2.new(0.02, 0, 0.02, 0)
    logo.BackgroundTransparency = 1
    logo.Image = "https://upload.wikimedia.org/wikipedia/commons/thumb/7/7f/AMD_Ryzen_logo.svg/120px-AMD_Ryzen_logo.svg.png"
    logo.Parent = leftPanel

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, -40, 0, 30)
    title.Position = UDim2.new(0, 40, 0, 0)
    title.BackgroundTransparency = 1
    title.Text = "RYZEN"
    title.TextColor3 = Color3.fromRGB(255, 0, 200)
    title.TextScaled = true
    title.Font = Enum.Font.GothamBold
    title.Parent = leftPanel

    local function createTabButton(text, yPos, tabName)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(1, 0, 0, 35)
        btn.Position = UDim2.new(0, 0, 0, yPos)
        btn.BackgroundColor3 = Color3.fromRGB(30, 0, 30)
        btn.BorderSizePixel = 0
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.TextScaled = true
        btn.Font = Enum.Font.Gotham
        btn.Parent = leftPanel
        
        btn.MouseButton1Click:Connect(function()
            selectedTab = tabName
            for _, child in ipairs(leftPanel:GetChildren()) do
                if child:IsA("TextButton") then
                    child.BackgroundColor3 = Color3.fromRGB(30, 0, 30)
                    child.TextColor3 = Color3.fromRGB(255, 255, 255)
                end
            end
            btn.BackgroundColor3 = Color3.fromRGB(80, 0, 80)
            btn.TextColor3 = Color3.fromRGB(255, 200, 255)
            updateContent()
        end)
        return btn
    end

    createTabButton("⚡ MAIN", 0.08, "MAIN")
    createTabButton("👁️ ESP", 0.16, "ESP")
    createTabButton("🎯 AIMBOT", 0.24, "AIMBOT")
    createTabButton("🚀 MOVEMENT", 0.32, "MOVEMENT")
    createTabButton("🔫 COMBAT", 0.40, "COMBAT")
    createTabButton("⚙️ MISC", 0.48, "MISC")

    -- ПРАВАЯ ПАНЕЛЬ (СОДЕРЖИМОЕ)
    local rightPanel = Instance.new("Frame")
    rightPanel.Size = UDim2.new(1, -200, 1, 0)
    rightPanel.Position = UDim2.new(0, 200, 0, 0)
    rightPanel.BackgroundColor3 = Color3.fromRGB(10, 0, 10)
    rightPanel.BorderSizePixel = 0
    rightPanel.Parent = mainFrame

    local contentFrame = Instance.new("ScrollingFrame")
    contentFrame.Size = UDim2.new(1, 0, 1, 0)
    contentFrame.BackgroundTransparency = 1
    contentFrame.BorderSizePixel = 0
    contentFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
    contentFrame.ScrollBarThickness = 5
    contentFrame.Parent = rightPanel

    -- ФУНКЦИЯ ОБНОВЛЕНИЯ КОНТЕНТА
    local function updateContent()
        for _, child in ipairs(contentFrame:GetChildren()) do
            child:Destroy()
        end
        
        local yPos = 0
        
        if selectedTab == "MAIN" then
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(1, 0, 0, 40)
            label.BackgroundTransparency = 1
            label.Text = "✦ RYZEN HUB ✦"
            label.TextColor3 = Color3.fromRGB(255, 0, 200)
            label.TextScaled = true
            label.Font = Enum.Font.GothamBold
            label.Parent = contentFrame
            yPos = yPos + 45
            
            local info = Instance.new("TextLabel")
            info.Size = UDim2.new(1, 0, 0, 30)
            info.Position = UDim2.new(0, 0, 0, yPos)
            info.BackgroundTransparency = 1
            info.Text = "Нажмите ПРАВЫЙ SHIFT для закрытия"
            info.TextColor3 = Color3.fromRGB(200, 200, 255)
            info.TextScaled = true
            info.Font = Enum.Font.Gotham
            info.Parent = contentFrame
            yPos = yPos + 35
            
            local info2 = Instance.new("TextLabel")
            info2.Size = UDim2.new(1, 0, 0, 25)
            info2.Position = UDim2.new(0, 0, 0, yPos)
            info2.BackgroundTransparency = 1
            info2.Text = "H - убить мардера | G - забрать пистолет"
            info2.TextColor3 = Color3.fromRGB(255, 200, 200)
            info2.TextScaled = true
            info2.Font = Enum.Font.Gotham
            info2.Parent = contentFrame
            yPos = yPos + 30
            
            contentFrame.CanvasSize = UDim2.new(0, 0, 0, yPos + 20)
            return
        end
        
        local function createToggle(text, default, callback)
            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 30)
            frame.Position = UDim2.new(0, 5, 0, yPos)
            frame.BackgroundColor3 = Color3.fromRGB(20, 0, 20)
            frame.BorderSizePixel = 0
            frame.Parent = contentFrame
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.6, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = Color3.fromRGB(255, 255, 255)
            label.TextScaled = true
            label.Font = Enum.Font.Gotham
            label.Parent = frame
            
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(0.2, 0, 0.7, 0)
            btn.Position = UDim2.new(0.75, 0, 0.15, 0)
            btn.BackgroundColor3 = default and Color3.fromRGB(80, 0, 80) or Color3.fromRGB(40, 0, 40)
            btn.BorderSizePixel = 0
            btn.Text = default and "ВКЛ" or "ВЫКЛ"
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.TextScaled = true
            btn.Font = Enum.Font.GothamBold
            btn.Parent = frame
            
            local state = default
            btn.MouseButton1Click:Connect(function()
                state = not state
                btn.BackgroundColor3 = state and Color3.fromRGB(80, 0, 80) or Color3.fromRGB(40, 0, 40)
                btn.Text = state and "ВКЛ" or "ВЫКЛ"
                callback(state)
                if text:find("ESP") then updateESP() end
                if text:find("FOV") then updateFOV() end
            end)
            
            yPos = yPos + 35
            return frame
        end
        
        local function createSlider(text, default, minVal, maxVal, callback)
            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 30)
            frame.Position = UDim2.new(0, 5, 0, yPos)
            frame.BackgroundColor3 = Color3.fromRGB(20, 0, 20)
            frame.BorderSizePixel = 0
            frame.Parent = contentFrame
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.5, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = Color3.fromRGB(255, 255, 255)
            label.TextScaled = true
            label.Font = Enum.Font.Gotham
            label.Parent = frame
            
            local slider = Instance.new("TextButton")
            slider.Size = UDim2.new(0.3, 0, 0.7, 0)
            slider.Position = UDim2.new(0.6, 0, 0.15, 0)
            slider.BackgroundColor3 = Color3.fromRGB(40, 0, 40)
            slider.BorderSizePixel = 0
            slider.Text = tostring(default)
            slider.TextColor3 = Color3.fromRGB(255, 255, 255)
            slider.TextScaled = true
            slider.Font = Enum.Font.GothamBold
            slider.Parent = frame
            
            local value = default
            slider.MouseButton1Click:Connect(function()
                local newVal = value + 10
                if newVal > maxVal then newVal = minVal end
                value = newVal
                slider.Text = tostring(value)
                callback(value)
            end)
            
            yPos = yPos + 35
            return frame
        end
        
        local function createColorPicker(text, default, callback)
            local frame = Instance.new("Frame")
            frame.Size = UDim2.new(1, -10, 0, 30)
            frame.Position = UDim2.new(0, 5, 0, yPos)
            frame.BackgroundColor3 = Color3.fromRGB(20, 0, 20)
            frame.BorderSizePixel = 0
            frame.Parent = contentFrame
            
            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.5, 0, 1, 0)
            label.BackgroundTransparency = 1
            label.Text = text
            label.TextColor3 = Color3.fromRGB(255, 255, 255)
            label.TextScaled = true
            label.Font = Enum.Font.Gotham
            label.Parent = frame
            
            local colors = {Color3.fromRGB(255,0,0), Color3.fromRGB(0,255,0), Color3.fromRGB(0,100,255), Color3.fromRGB(255,255,0), Color3.fromRGB(255,0,255)}
            local currentIndex = 1
            local colorBtn = Instance.new("TextButton")
            colorBtn.Size = UDim2.new(0.2, 0, 0.7, 0)
            colorBtn.Position = UDim2.new(0.7, 0, 0.15, 0)
            colorBtn.BackgroundColor3 = colors[1]
            colorBtn.BorderSizePixel = 0
            colorBtn.Text = ""
            colorBtn.Parent = frame
            
            colorBtn.MouseButton1Click:Connect(function()
                currentIndex = currentIndex % #colors + 1
                colorBtn.BackgroundColor3 = colors[currentIndex]
                callback(colors[currentIndex])
            end)
            
            yPos = yPos + 35
            return frame
        end
        
        if selectedTab == "ESP" then
            createToggle("ESP ВКЛ", settings.espEnabled, function(s) settings.espEnabled = s updateESP() end)
            
            local modeFrame = Instance.new("Frame")
            modeFrame.Size = UDim2.new(1, -10, 0, 30)
            modeFrame.Position = UDim2.new(0, 5, 0, yPos)
            modeFrame.BackgroundColor3 = Color3.fromRGB(20, 0, 20)
            modeFrame.BorderSizePixel = 0
            modeFrame.Parent = contentFrame
            
            local modeLabel = Instance.new("TextLabel")
            modeLabel.Size = UDim2.new(0.5, 0, 1, 0)
            modeLabel.BackgroundTransparency = 1
            modeLabel.Text = "Режим ESP"
            modeLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            modeLabel.TextScaled = true
            modeLabel.Font = Enum.Font.Gotham
            modeLabel.Parent = modeFrame
            
            local modeBtn = Instance.new("TextButton")
            modeBtn.Size = UDim2.new(0.3, 0, 0.7, 0)
            modeBtn.Position = UDim2.new(0.6, 0, 0.15, 0)
            modeBtn.BackgroundColor3 = Color3.fromRGB(40, 0, 40)
            modeBtn.BorderSizePixel = 0
            modeBtn.Text = settings.espMode
            modeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            modeBtn.TextScaled = true
            modeBtn.Font = Enum.Font.GothamBold
            modeBtn.Parent = modeFrame
            
            local modes = {"Роли", "Имена", "Все"}
            local modeIndex = 1
            modeBtn.MouseButton1Click:Connect(function()
                modeIndex = modeIndex % #modes + 1
                settings.espMode = modes[modeIndex]
                modeBtn.Text = settings.espMode
                updateESP()
            end)
            yPos = yPos + 35
            
            createToggle("ESP Имен", settings.espNames, function(s) settings.espNames = s updateESP() end)
            createToggle("Шкала здоровья", settings.espHealth, function(s) settings.espHealth = s updateESP() end)
            createSlider("Прозрачность", 5, 1, 10, function(v) settings.espTransparency = v / 10 updateESP() end)
            createColorPicker("Цвет ESP", settings.espColor, function(c) settings.espColor = c updateESP() end)
            
        elseif selectedTab == "AIMBOT" then
            createToggle("Aimbot ВКЛ", settings.aimbotEnabled, function(s) settings.aimbotEnabled = s toggleAimbot(s) end)
            createToggle("Auto Shoot", settings.autoShoot, function(s) settings.autoShoot = s end)
            createToggle("FOV Circle", settings.fovCircle, function(s) settings.fovCircle = s updateFOV() end)
            createSlider("FOV Radius", 150, 50, 300, function(v) settings.fovRadius = v updateFOV() end)
            createSlider("Smoothness", 5, 1, 20, function(v) settings.smoothness = v end)
            createColorPicker("FOV Color", settings.fovColor, function(c) settings.fovColor = c updateFOV() end)
            
            local partFrame = Instance.new("Frame")
            partFrame.Size = UDim2.new(1, -10, 0, 30)
            partFrame.Position = UDim2.new(0, 5, 0, yPos)
            partFrame.BackgroundColor3 = Color3.fromRGB(20, 0, 20)
            partFrame.BorderSizePixel = 0
            partFrame.Parent = contentFrame
            
            local partLabel = Instance.new("TextLabel")
            partLabel.Size = UDim2.new(0.5, 0, 1, 0)
            partLabel.BackgroundTransparency = 1
            partLabel.Text = "Aim Part"
            partLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
            partLabel.TextScaled = true
            partLabel.Font = Enum.Font.Gotham
            partLabel.Parent = partFrame
            
            local partBtn = Instance.new("TextButton")
            partBtn.Size = UDim2.new(0.3, 0, 0.7, 0)
            partBtn.Position = UDim2.new(0.6, 0, 0.15, 0)
            partBtn.BackgroundColor3 = Color3.fromRGB(40, 0, 40)
            partBtn.BorderSizePixel = 0
            partBtn.Text = settings.aimPart
            partBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            partBtn.TextScaled = true
            partBtn.Font = Enum.Font.GothamBold
            partBtn.Parent = partFrame
            
            local parts = {"Head", "HumanoidRootPart", "Torso"}
            local partIndex = 1
            partBtn.MouseButton1Click:Connect(function()
                partIndex = partIndex % #parts + 1
                settings.aimPart = parts[partIndex]
                partBtn.Text = settings.aimPart
            end)
            yPos = yPos + 35
            
        elseif selectedTab == "MOVEMENT" then
            createToggle("Speed", false, function(s) toggleSpeed(s) end)
            createSlider("Speed Value", 60, 20, 300, function(v) settings.speed = v end)
            createToggle("Bunny Hop", false, function(s) toggleBunnyHop(s) end)
            createToggle("Super Jump", false, function(s) toggleJump(s) end)
            createSlider("Jump Power", 120, 50, 300, function(v) settings.jumpPower = v end)
            createToggle("Fly", false, function(s) toggleFly(s) end)
            createSlider("Fly Speed", 50, 10, 200, function(v) settings.flySpeed = v end)
            createToggle("Noclip", false, function(s) toggleNoclip(s) end)
            createToggle("Teleport", false, function(s) toggleTeleport(s) end)
            
        elseif selectedTab == "COMBAT" then
            createToggle("Trigger Bot", false, function(s) toggleTriggerBot(s) end)
            createToggle("Shot Murderer [H]", false, function(s) toggleShotMurderer(s) end)
            createToggle("Авто-подбор пистолета", false, function(s) toggleAutoCollectGun(s) end)
            
            local pickupBtn = Instance.new("TextButton")
            pickupBtn.Size = UDim2.new(0.8, 0, 0, 35)
            pickupBtn.Position = UDim2.new(0.1, 0, yPos, 0)
            pickupBtn.BackgroundColor3 = Color3.fromRGB(60, 0, 60)
            pickupBtn.BorderSizePixel = 0
            pickupBtn.Text = "🔫 Забрать пистолет [G]"
            pickupBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            pickupBtn.TextScaled = true
            pickupBtn.Font = Enum.Font.GothamBold
            pickupBtn.Parent = contentFrame
            pickupBtn.MouseButton1Click:Connect(pickupGun)
            yPos = yPos + 40
            
        elseif selectedTab == "MISC" then
            createToggle("FPS", false, function(s) toggleFps(s) end)
            
            local closeBtn = Instance.new("TextButton")
            closeBtn.Size = UDim2.new(0.8, 0, 0, 35)
            closeBtn.Position = UDim2.new(0.1, 0, yPos, 0)
            closeBtn.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
            closeBtn.BorderSizePixel = 0
            closeBtn.Text = "❌ Закрыть меню"
            closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
            closeBtn.TextScaled = true
            closeBtn.Font = Enum.Font.GothamBold
            closeBtn.Parent = contentFrame
            closeBtn.MouseButton1Click:Connect(function() gui:Destroy() end)
            yPos = yPos + 40
        end
        
        contentFrame.CanvasSize = UDim2.new(0, 0, 0, yPos + 20)
    end

    updateContent()
    print("✅ Ryzen Hub [MAUS STYLE] загружен!")
end

-- ===== ОТКРЫТИЕ ПО ПРАВОМУ SHIFT =====
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.RightShift then
        if gui and gui.Parent then
            gui:Destroy()
            gui = nil
        else
            createRyzenHub()
        end
    end
end)

-- Обработчики клавиш
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.H then
        killMurderer()
    end
    if input.KeyCode == Enum.KeyCode.G then
        pickupGun()
    end
end)

createRyzenHub()
print("✅ RYZEN HUB готов! Нажми ПРАВЫЙ SHIFT.")B